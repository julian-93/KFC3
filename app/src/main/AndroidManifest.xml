<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kfc.julianalmanza.kfc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:logo="@drawable/titulo_kfc"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Miperfil" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Loggin" />
        <activity android:name=".Registrarse" />
        <activity android:name=".Productos" />
        <activity android:name=".visualizacion_productos" />
        <activity android:name=".Drawer"></activity>
    </application>

</manifest>